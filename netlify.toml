[build]
  publish = "dist/budeme-prochazkovi/browser"
  command = "npm ci && npx ng build --configuration production"
[build.environment]
  NODE_VERSION = "18"
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
